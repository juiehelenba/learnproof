{
  "name": "learnproof-blockchain",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "anchor": "node cli.mjs anchor",
    "verify": "node cli.mjs verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  }
}
